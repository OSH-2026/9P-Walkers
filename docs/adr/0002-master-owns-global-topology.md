# 主机维护全局拓扑图

主机维护节点身份、节点地址、节点名和链路关系组成的全局拓扑图，并从图中派生下一跳路由；从机只维护本地邻居状态和主机下发的简化转发表。这个边界让故障重算、路径选择和诊断集中在资源更充足的主机侧，避免每个 STM32 从机同步完整拓扑或运行全局路由算法。

**状态**：accepted

**考虑过的方案**：所有节点维护完整拓扑图、所有节点只维护下一跳表、主机维护全局拓扑图并下发从机转发表。最终选择第三种，因为它兼顾主机的全局可见性和从机侧实现的静态内存约束。
