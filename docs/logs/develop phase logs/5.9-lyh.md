- 搭建极小从机 backend：当前只暴露 `/`、`/sys`、`/sys/health`，暂未接入 littlefs。
- 完成从机侧 Mini9P 串口联调模式组装：`uart_transport + mini9p_server + local_vfs`。
- 上板 STM32F407ZGT6，使用 `tools/pc_master_emulator` 模拟 master 与从机通信。
  - 运行命令：`./pc_master_emulator /dev/ttyUSB0 1000000`
  - 主流程已通过：`Tattach -> Twalk /sys/health -> Topen -> Tread -> Tclunk`
  - `/sys/health` 可读到 `ok\n`
  - 错误路径补测通过：不存在路径返回 `ENOENT`，非法 fid 返回 `EFID`
- 测试记录见：`docs/logs/pc_master_emulate/5-9.md`。

