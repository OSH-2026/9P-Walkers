- vfs 最小版本实现，支持主从传输，不支持其他连接（这个没太想好）
- 建立vfs虚拟测试，ai认为通过，暂未人工审查
- vfs 调用接口见子目录下的接口说明

如果以后做中继，有两个选择：

- 方案 A：继续用路径携带 target
/mcu3/dev/temp 原样发给下一跳，让下一跳解析 /mcu3

- 方案 B：加外层 routing header，例如
```c
dst_node=mcu3 // 目标节点
ttl=...  //Time To Live,这个包最多还能被转发多少跳
payload=原始 Mini9P frame
```