# 5.24 ESP32-p4主机 vfs与Lua运行时绑定

> Author: wwt

组里俩人去打 RoboMaster 了，主机板子不在我手里，做的提交还没有测试，需要等 25 号霍斌回来了才能做。

主要测试：

- WebShell 是否可以正确建立 WiFi 热点，是否可以在别的设备通过连接热点进行访问
- esp32p4 主机是否可以在 IDF 上编译并烧录，Lua 进行时是否与 vfs 接口正确连接，我们是否可以正确进行主机和从机的连接？
- 接下来要进一步细化主机和从机的任务调度、分发等细节，争取能在web shell 上看到正确的文件树